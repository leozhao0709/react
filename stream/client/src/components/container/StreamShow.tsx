import * as React from 'react';

interface StreamShowProps extends React.HtmlHTMLAttributes<{}> {}

const StreamShow: React.FC<StreamShowProps> = (props: StreamShowProps) => {
  return <React.Fragment>StreamShow</React.Fragment>;
};

StreamShow.defaultProps = {};

export default StreamShow;
