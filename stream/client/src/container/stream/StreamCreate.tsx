import * as React from 'react';

interface StreamCreateProps extends React.HtmlHTMLAttributes<{}> {}

const StreamCreate: React.FC<StreamCreateProps> = (props: StreamCreateProps) => {
  return <React.Fragment>StreamCreate</React.Fragment>;
};

StreamCreate.defaultProps = {};

export default StreamCreate;
