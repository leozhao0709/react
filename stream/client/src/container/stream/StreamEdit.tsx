import * as React from 'react';

interface StreamEditProps extends React.HtmlHTMLAttributes<{}> {}

const StreamEdit: React.FC<StreamEditProps> = (props: StreamEditProps) => {
  return <React.Fragment>StreamEdit</React.Fragment>;
};

StreamEdit.defaultProps = {};

export default StreamEdit;
